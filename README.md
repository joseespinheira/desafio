Requisito: node e npm instalado e configurado no computador.

Comando para instalar as dependências:
- npm install

Comando para iniciar o serviço na porta 3000:
- npm start

Depois basta acessa pelo ip ou localhost no navegador, ex:
http://localhost:3000/